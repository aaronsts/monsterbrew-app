Requirements:

- When the user selects a CR, have a button 'recommended stats' that opens a dialog to display the recommended stats along with it (AC, DC, damage per round, HP, Ability Modifiers)
- Next to the related fields, have a small icon + description saying: high, on par, low
- Show a delta chart at the top of the page (that can be hidden away) that reflects the current state of the creature relative to the chosen CR.
- Use the cr-benchmarks.ts file and the formulas found in quick-reference section.
- Add an editor wide toggle to turn of CR suggestion, I want all this logic to be incapsulated into it's own components, so we can easily drop in the component in the current form.
